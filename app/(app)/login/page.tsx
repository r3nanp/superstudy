import { Login } from "../components/Login";

export const metadata = {
  title: "Login | superstudy",
  description: "Faça login e comece a estudar",
};

export default function LoginPage() {
  return <Login />;
}
